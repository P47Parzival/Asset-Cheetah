name: data
description: Data layer (models, repositories, API clients).
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  dio: ^5.0.0
  flutter_secure_storage: ^8.0.0
  flutter_riverpod: ^2.4.0
  riverpod_annotation: ^2.2.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  isar_generator: ^3.1.0
