name: inventory_web
description: A new Flutter web project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../packages/core
  data:
    path: ../../packages/data
  auth:
    path: ../../packages/features/auth
  inventory:
    path: ../../packages/features/inventory
  reports:
    path: ../../packages/features/reports
  common_widgets:
    path: ../../packages/features/common_widgets
  go_router: ^12.0.0
  flutter_riverpod: ^2.4.0
  google_fonts: ^6.1.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
